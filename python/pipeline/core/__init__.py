"""Core of the rule-driven plugin pipeline (ADR-073).

Domain-ignorant building blocks: the plugin contract, the forward Context,
injected Services, the fault/abort model, and the generic Orchestrator.
"""
