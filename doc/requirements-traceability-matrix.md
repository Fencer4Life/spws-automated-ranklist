# Requirements Traceability Matrix (RTM)

*Externalized from Project Specification Appendix C in Phase 0.5 (2026-05-01). The spec retains Appendix C as the Architecture Decision registry only.*

Every functional and non-functional requirement is listed below with its source section and verifying test(s). Test IDs reference the development plan test numbering; test files are in `supabase/tests/`, `python/tests/`, and `frontend/tests/`.

For ADR-to-FR cross-references, see Project Specification Appendix C — Architecture Decisions.

## Functional Requirements

| ID | Requirement | Source | Tests | Status |
|----|-------------|--------|-------|--------|
| FR-01 | Scrape results from FTL (JSON + CSV), Engarde (HTML), 4Fence (HTML) | UC1(a) | 3.1a–g, 3.2a–f, 3.3a–e | Covered |
| FR-02 | Import results via CSV upload | UC2(a,b) | 3.7 | Covered |
| FR-03 | Auto-detect scraper platform from URL | UC1(a) | 3.1–3.3 URL dispatcher tests | Covered |
| FR-04 | Fuzzy match scraped names to master fencer list | UC3(a–f) | 4.1–4.5 | Covered |
| FR-05 | Alias matching (json_name_aliases) | UC3(a,b) | 4.2 (alias tests) | Covered |
| FR-06 | Duplicate name disambiguation via age category | §8.5(5) | 4.25–4.37 | Covered |
| FR-07 | Admin approve / dismiss / create-new-fencer for match candidates | UC4(b,c) | 4.6–4.8, 11.13–11.16 | Covered |
| FR-08 | Domestic intake: auto-create fencer for UNMATCHED PPW/MPW | §8.5 | 4.10–4.14b, 9.142–9.148 | Covered |
| FR-09 | International intake: skip UNMATCHED PEW/MEW fencers | §8.5 | 4.15–4.18 | Covered |
| FR-10 | Birth year estimation (youngest boundary per category) | §8.5 | 4.19–4.21, 9.83–9.84 | Covered (M9, T9.9) |
| FR-11 | Score computation: place points (log formula) | §8.1.1 | 2.1–2.3, 2.2a–b | Covered |
| FR-12 | Score computation: DE round bonus | §8.1.2 | 2.4–2.5 | Covered |
| FR-13 | Score computation: podium bonus | §8.1.3 | 2.6a–d | Covered |
| FR-14 | Tournament multipliers (PPW, MPW, PEW, MEW, MSW, PSW) | §8.2 | 2.7, 2.19, 9.85 | Covered (M9, T9.9) |
| FR-15 | PPW ranking: best-K domestic + always-include MPW; `p_rolling` parameter for active-season carry-over (ADR-018) | §8.3.1 | 5.2–5.13, 5.16, 5.20–5.21, 5.23, R.4–R.12 | Modified (M10, rolling parameter) |
| FR-16 | Kadra ranking: domestic + best-J international pool; `p_rolling` parameter for active-season carry-over (ADR-018) | §8.3.2 | 5.17, 5.19, 5.22, R.13–R.14 | Modified (M10, rolling parameter) |
| FR-17 | V0 guard: no Kadra ranking for V0 | §8.3.2 | 5.18 | Covered |
| FR-18 | Cross-category carryover (fencer ranked by birth-year category) | §8.5(2) | 5.14–5.15 | Covered |
| FR-19 | JSONB bucket-based ranking rules | §8.6.6 | 5.4–5.7, 5.20–5.22 | Covered |
| FR-20 | Legacy code path (NULL json_ranking_rules) | §8.6.6 | 5.23 | Covered |
| FR-21 | Season lifecycle: create season, auto-create scoring config (inherits json_ranking_rules from previous season), auto-activate by date (ADR-031) | UC7(a,b) | 1.13–1.14b, 9.40–9.46 | Covered |
| FR-22 | Single active season constraint (auto-derived, no overlapping dates) | UC7(c) | 1.7, 1.15, 9.41–9.46 | Covered |
| FR-23 | Event lifecycle state machine | UC10(a,b) | 1.20–1.24, 9.86–9.90 | Covered (M9, T9.9) |
| FR-24 | Audit logging for status changes | UC10(c) | 1.22a–b | Covered |
| FR-25 | Tournament multiplier auto-population (trigger) | UC9(c) | 1.19, 1.19c | Covered |
| FR-26 | Scoring config export/import (JSON) | UC18, UC19 | 2.12–2.18 | Covered |
| FR-27 | Calibration comparison against reference Excel | UC20 | 2.19–2.23 (pytest) | Covered |
| FR-28 | Minimum participant threshold (configurable per type) | §8.5(1) | 3.10–3.11 | Covered |
| FR-29 | Idempotent re-import (skip existing results) | §9.5.1 | 3.9 | Covered |
| FR-30 | Retry on transient network failure | §9.5.1 | 3.12 | Covered |
| FR-31 | Partial scrape detection (abort on incomplete data) | §9.5.1 | 3.13, 3.13b | Covered |
| FR-32 | Telegram alerting on pipeline failure | UC1(d) | 3.6 | Covered |
| FR-33 | Web Component: weapon/gender/category filters | UC12(b) | FilterBar.test.ts (5 tests) | Covered |
| FR-34 | Web Component: PPW/Kadra toggle with V0 guard (conditional on `bool_show_evf_toggle` season config, ADR-017) | UC12(b) | FilterBar.test.ts, DrilldownModal.test.ts | Modified (toggle hidden by default, visible when season config enables it) |
| FR-35 | Web Component: ranking table with mode-specific columns | UC12(c) | RanklistTable.test.ts (5 tests) | Covered |
| FR-36 | Web Component: fencer drilldown modal with score breakdown | UC13(a,b) | DrilldownModal.test.ts (24 tests) | Covered |
| FR-37 | ODS export (ranking + drilldown) | UC12, UC13 | export.test.ts (5 tests) | Covered |
| FR-38 | API client: fetch seasons, rankings, fencer scores | UC12(a) | 5.1a–b, api.test.ts (6 tests) | Covered |
| FR-39 | EN/PL internationalisation with reactive toggle | §11 | DrilldownModal.test.ts (test K) | Covered |
| FR-40 | Import status transitions (PLANNED → IMPORTED → SCORED) | UC1(b), UC5(c) | 2.9, 9.91–9.92 | Covered (M9, T9.9) |
| FR-41 | Domestic-participation requirement: fencers with 0 domestic points excluded from ranking views | §8.5(7) | 5.24–5.25 | Covered |
| FR-42 | CERT/PROD environment toggle with runtime switching | §2.2 | 8.01–8.04 | Covered (M8) |
| FR-43 | Calendar view: chronological event list with season filter | UC21(a,b) | 8.11–8.19, 8.38–8.43, 8.47 | Covered (M8) |
| FR-44 | Calendar view: past/future/all toggle (scope filter conditional on `bool_show_evf_toggle`, ADR-017) | UC21(c) | 8.44–8.45, 8.79–8.80 | Modified (M8, scope filter hidden by default) |
| FR-45 | Calendar view: mobile-friendly layout | UC21(e) | 8.46 | Covered (M8) |
| FR-46 | Admin authentication: Supabase Auth + TOTP MFA (supersedes client-side password gate) | UC22(a), ADR-016 | 9.01–9.17 | Covered (M9, T9.0) |
| FR-47 | Season CRUD via web UI | UC22(b) | 9.18–9.22, 9.27, 9.37–9.42 | Covered (M9, T9.1 SQL + T9.2 UI) |
| FR-48 | tbl_event schema extension: 12 columns (txt_country, txt_venue_address, url_invitation, num_entry_fee, txt_entry_fee_currency, arr_weapons, url_registration, dt_registration_deadline, url_event_2, url_event_3, url_event_4, url_event_5) | UC22(c), UC21(d), ADR-030, ADR-040 | 8.05–8.10, 8.18–8.20, 15.1 | Covered (M8+M9+ADR-030+ADR-040) |
| FR-49 | Tournament CRUD nested under events | UC22(d) | 9.25–9.26, 9.29, 9.50–9.55 | Covered (M9, T9.1 SQL + T9.4 UI) |
| FR-50 | Delete cascade (event → tournaments → results) | UC22(e) | 9.30–9.36 | Covered (M9, T9.1) |
| FR-51 | Tournament re-import in single transaction | UC23(a-f) | 10.1–10.7 | Covered (ADR-022, fn_ingest_tournament_results) |
| FR-52 | Multi-category expansion (30 sub-rankings) | §6.2 | 8.20–8.26 | Covered (M8) |
| FR-53 | Event-level batch import: multi-select tournament checklist modal. **N14 (ADR-073 amendment, 2026-06-18):** tournament `url_results` is now populated **inline during ingestion** — the `Commit` plugin writes each committed tournament's parsed results-page URL via `fn_find_or_create_tournament(p_url_results)`, gated on WEB source kinds (FTL/ENGARDE/FOURFENCE/DARTAGNAN/OPHARDT_HTML + http source_url); re-ingest overwrites, XML/EVF-API/file preserve. The drilldown link works without the flaky dispatch button (which remains a manual fallback; `populate_tournament_urls.py` ophardt branch still pending). | UC22(g), ADR-073 | 9.62–9.67, N14.4–N14.8, 46.1–46.5 | Partial (inline-ingestion populate ✓; file UI + Admin ⬇ button + populate-urls GHA) |
| FR-54 | Tournament-level single import: own URL or file upload (FTL / Engarde / 4fence / Dartagnan) | UC22(h) | 9.56–9.61, 3.17a–d, dart.1–dart.8, dart.url.1 | Partial (file UI + Admin ⬇ button + t-scrape + scrape-tournament GHA + Dartagnan parser) |
| FR-55 | File import: parse results from .xlsx, .xls, .json, .csv | UC22(i), UC23(c) | 9.58, 9.93–9.100 | Covered (M9, T9.5 UI + T9.10 parsers) |
| FR-56 | Identity resolution admin UI: match candidate queue with approve/dismiss/create-new/assign + gender column; Identities tab in Fencers view (ADR-035) | UC4(a-e) | 9.68–9.73, 9.77, 9.78–9.88, 11.1–11.19 | Covered (UI + RPCs + tab in App.svelte) |
| FR-57 | Identity resolution: disambiguation modal for same-name fencers with age category fit | UC3(f), UC4(b) | 9.74–9.76 | Covered (DisambiguationModal + App.svelte handlers) |
| FR-58 | EVF calendar import: HTML-primary fetch from veteransfencing.eu with JSON-API cross-reference, event-level URL harvesting (`url_event`, `url_invitation`, `url_registration`), dedup, create events via `fn_import_evf_events_v2` (allocator-driven code, EVF organizer, FK auto-link, idempotent), refresh existing events via `fn_refresh_evf_event_urls` (NULL-only, protects admin edits), raise on total failure. Deadline harvesting disabled pending real-world pattern data. Dedup algorithm rev 3 (ADR-039 2026-04-25): name comparison removed, location step added, single matcher across calendar + results paths. Code allocator (ADR-043 2026-04-26): three-step ladder (CURRENT_SLOT_REUSE → PRIOR_SEASON_MATCH → NEXT_FREE_ALLOC) + classifier (PEW/IMEW/DMEW; MEW dropped); Telegram alert per NEXT_FREE_ALLOC. Automated CERT→PROD propagation via `promote.py --mode calendar` (see FR-86). | UC8, UC9 | 12.1–12.13, pytest evf.1–evf.21, evf.24, evf.40–evf.42, evf.25–evf.39, prom.5–prom.7 | Covered (ADR-028 rev 3 2026-04-26 → ADR-043: allocator + classifier + EVF organizer; ADR-039: dedup ladder; ADR-026 calendar mode) |
| FR-59 | Two-view app shell: sidebar drawer with Ranklista + Kalendarz navigation | UC12, UC21 | 8.27–8.37 | Covered (M8) |
| FR-60 | Event CRUD via web UI (create, edit, delete events with all fields) | UC22(c) | 9.23–9.24, 9.28, 9.43–9.49 | Covered (M9, T9.1 SQL + T9.3 UI) |
| FR-61 | Scoring config editor (admin, per-season, structured form) | UC22(f) | 8.62–8.75 | Covered (M8) |
| FR-62 | Calendar view: completed events show "Wyniki" link to event results URL | UC21 | 8.76–8.77 | Covered (M8) |
| FR-63 | Calendar event links stacked vertically: Wyniki and Komunikat organizatora rendered one below the other | UC21 | 8.78 | Covered (ADR-017) |
| FR-64 | Season-level EVF toggle config: `bool_show_evf_toggle` in `tbl_scoring_config` controls PPW/+EVF toggle visibility in Ranklist and Calendar; admin checkbox in SeasonManager edit form | ADR-017 | 9.37–9.39, 8.79–8.83 | Covered |
| FR-65 | Rolling ranking for active season only: `p_rolling BOOLEAN DEFAULT FALSE` on `fn_ranking_ppw` and `fn_ranking_kadra`; position-matched carry-over from previous season; declared-counterpart constraint; category crossing via current season's end year. Future (not yet active) seasons intentionally show empty ranklist — rolling only kicks in when the season becomes active (ADR-031). **Amended 2026-06-26 (ADR-018/021):** carry-stop is results-based (a current-season scored result for a position, weapon+gender scoped) not status-based — strict either/or, never both. | ADR-018, ADR-031, §8.3.1, §8.3.2 | R.1–R.14, R.24 | Covered |
| FR-66 | Rolling drilldown: `fn_fencer_scores_rolling` returns carried-over scores with `bool_carried_over` flag and source season code; visual distinction in DrilldownModal (grey striped bars, `↩` marker, rolling info banner) and CalendarView (progress slot bar). **Amended 2026-06-26 (ADR-018/021):** never shows both this-year and last-year for the same position — the carried row drops the moment a current-season result exists. | ADR-018 | R.15–R.21, R.22–R.24 (pgTAP, results-based carry-stop) | Covered |
| FR-67 | Birth year range subtitle on ranklist view: displays eligible birth years for selected age category and season as enumeration (e.g. `kat. 2 — roczniki: 1976, 1975, .. 1967`); V4 open-ended ("i starsi"/"and older"); updates on category/season/locale change; PL+EN | UC12 | BY.1–BY.7 | Covered |
| FR-68 | Biennial event carry-over: rolling carry-over uses rules-based type matching (`json_ranking_rules` buckets) instead of declared-event matching; IMEW (type=MEW) results from previous season automatically carry over when MEW is in the international rules, even without an IMEW event in the current season (ADR-021) | ADR-021 | R.19–R.21 | Covered |
| FR-70 | Orchestrator parses FTL XML metadata and routes to correct DB tournament by weapon+gender+category+date; unified path via `ingest_xml_unified` routes through `run_pipeline` + DraftStore + `fn_commit_event_draft` (ADR-067 retires legacy `process_xml_file`) | ADR-022, ADR-067, §2.5 | 9.149–9.151, 67.U1–67.U4 (unified ingest entry point + per-file URL fragments + deprecation) | Covered |
| FR-71 | Orchestrator splits combined-category XML (v0v1, v0v1v2) into per-category result sets with re-ranking; structural pool-only skip drops ELIMINACJE qualifier files (`<Poule>` without `<Tableau>`) at the parser so they never reach the splitter (ADR-067); Stage-0 roster reconciliation creates genuinely-new combined-pool participants up front (high-precision exact, not fuzzy) so they are not glued to existing names, and reconciles a conflicting birth year to the bracket V-cat — promotion → band youngest edge, demotion → midpoint (ADR-056 Stage-0, amend 2026-06-26) | ADR-024, ADR-056, ADR-067 | 9.152–9.154, 67.P1–67.P8 (pool-only structural skip), S0.1–S0.9 + 10.4.1/10.4.2/10.4.2b (pytest `test_s0_reconcile_roster`: midpoint/authoritative-vcat/exact-create/promotion-edge+demotion-midpoint/international-skip/idempotence/cross-bracket-conflict/staging-md) | Covered |
| FR-72 | Orchestrator resolves fencer identities via fuzzy matching against DB (domestic auto-create, international skip) | UC3, ADR-020 | 9.155–9.157 | Covered |
| FR-73 | `fn_ingest_tournament_results` atomically deletes old + inserts new results + scores in single transaction | ADR-014, ADR-022 | 10.1–10.7 | Covered |
| FR-74 | CLI entry point processes single XML files, .zip archives, or Supabase Storage staging bucket | ADR-023 | 9.166–9.168 | Covered |
| FR-75 | Google Apps Script extracts email .zip attachments from Gmail → uploads to Supabase Storage staging | ADR-023 | Manual E2E ✓ | Covered |
| FR-76 | GitHub Actions `ingest.yml` downloads from Storage staging → processes → archives | ADR-023 | Manual E2E ✓ | Covered |
| FR-77 | Processed .zip archived to `archive/{season}/{event}.zip`; staging cleaned; previous event compressed | ADR-023 | 9.169–9.172, 9.191–9.192 | Covered |
| FR-78 | Telegram notifications for all pipeline events (13 use cases: routine, warnings, alerts, overdue) | ADR-025 | 9.173–9.190 | Covered |
| FR-79 | Event-centric ingestion: match XML to existing event by date, create tournaments on-the-fly | ADR-025 | 10.8–10.12, 9.193 | Covered |
| FR-80 | Event status lifecycle: PLANNED → IN_PROGRESS → COMPLETED with rollback | ADR-025 | 10.12–10.15 | Covered |
| FR-81 | Telegram command interface: 20+ admin commands for lifecycle, review, storage, season, PROD, URLs, emergency | ADR-025 | 10.16–10.22 | Covered |
| FR-82 | CERT → PROD promotion triggered from Telegram and Admin UI | ADR-026 | PPW4 E2E ✓ | Covered |
| FR-83 | Batch summary notification after each ingestion run | ADR-025 | 9.197 | Covered |
| FR-84 | ADR-024 compliance: flag PENDING for unknown DOB in combined categories | ADR-024/025 | 9.194–9.196 | Covered |
| FR-85 | Workflow failure notifications via Telegram | ADR-025 | 9.202–9.203 | Covered |
| FR-86 | CERT → PROD promotion: per-tournament transfer with url_results carry and error recovery (event mode), plus automated EVF calendar propagation (calendar mode — new events + URL refresh via `fn_import_evf_events` / `fn_refresh_evf_event_urls`; runs after 3-day EVF scrape cron, admin edits protected, concurrency-group-protected from overlap with event-promote) | ADR-026 (rev 2026-04-20), ADR-028 | 9.204–9.207, prom.5–prom.7 | Covered |
| FR-87 | Auto-export seed SQL files after promotion (committed to repo) | ADR-026 | 9.208 | Superseded by FR-88 |
| FR-88 | Monolithic PROD seed export on promote (schema-driven, single file) | ADR-027→ADR-036 | 9.209–9.213 | Covered |
| FR-89 | Auto-resume email polling on event day | ADR-027 | GAS E2E ✓ | Covered |
| FR-90 | Event registration URL: nullable `url_registration` on `tbl_event`, displayed in Calendar before deadline/start, editable in Admin UI | UC21, ADR-030 | 8.18–8.20, 8.21–8.25, 9.43a–9.43c | Covered |
| FR-91 | Event registration deadline: nullable `dt_registration_deadline` on `tbl_event`, displayed in Calendar until deadline passes, editable in Admin UI | UC21, ADR-030 | 8.18–8.20, 8.21–8.25, 9.43a–9.43c | Covered |
| FR-92 | Fencer gender column with backfill from tournament participation, inline admin edit, gender mismatch highlighting in Identity Manager, and automated cross-gender scoring enforcement (ADR-034) | ADR-033, ADR-034 | 11.16–11.19, 9.85–9.86, 9.89–9.94, 14.CG1–14.CG9 | Covered |
| FR-93 | Birth year review tab: filter/search/edit/tournament history grouped by season/birth year hints + auto-suggest/age category inconsistency flag (now fires for estimated/downgraded BYs too — ADR-056 Stage-0); birth-year estimate convention is the band MIDPOINT | UC16, ADR-035, ADR-056 | 9.100–9.113, 9.111b, 13.1–13.4, 43.1–43.9 (Stage-0 RPC/flag transitions + audit), vitest 5.1 (midpoint) | Covered |
| FR-94 | Derived event display status: PLANNED events whose `dt_end < today` render as amber "Awaiting results" / "Oczekiwanie na wyniki" instead of misleading "Planowany". View-layer helper only — underlying `enum_status` unchanged, preserving ADR-018 rolling carry-over invariant. Self-heals when `fn_ingest_tournament_results` fires. | UC21, ADR-037 | ES.1–ES.11, 8.41b | Covered |
| FR-95 | Event deletion admin tool: `fn_delete_event(prefix)` RPC (+ Telegram `delete <prefix>` command) performs rollback + removal of `tbl_event` row in a single transaction. Used when an event was created in error (wrong-ingest, erroneous scrape, dedup-bug phantom). Stricter than `rollback` (which keeps the event row for re-ingest). Prefix-matches in the active season, reuses `_resolve_event_prefix` + `fn_delete_tournament_cascade` helpers. Admin-only (REVOKE anon, GRANT authenticated). | UC22(e), UC27, ADR-025 (amendment 2026-04-21) | 10.29–10.32 | Covered |
| FR-96 | EVF stale-event gate: scraper does not auto-create or auto-update events outside the 30-day fresh window or marked `enum_status='COMPLETED'`. `is_in_scope(event)` predicate is applied to existing CERT rows AND scraped EVF events before passing them to `_find_existing_match` / `_create_cert_event`. Stale events are admin-territory; the cron only ever touches in-flight (≤30 days post-end, not COMPLETED) rows. Implemented in `python/scrapers/evf_calendar.py` (`is_in_scope`, `STALE_WINDOW_DAYS`) and applied at entry of `sync_calendar` / `sync_results` in `evf_sync.py`. | UC25, ADR-039 | pytest evf.22, evf.24 | Covered |
| FR-97 | EVF logical-integrity guard: a `tbl_event` row with `dt_start > today AND enum_status = 'COMPLETED'` is data corruption. **ADR-039 rev 2 (2026-06-13):** `sync_calendar` self-heals each violator via `_heal_future_completed` — recover the real date from the **event URL** (`url_event` → EVF-calendar match → FTL fallback; authed FTL session, ADR-069; `fetch_ftl_event_metadata` accepts `eventSchedule` + `results` forms), then rewrite `dt_start`/`dt_end`, `dt_tournament` and **populate `url_event`** (status stays COMPLETED). When no date is recoverable, demote `enum_status` COMPLETED → PLANNED (bypassing the terminal-COMPLETED lifecycle trigger) so the guard clears. `compute_future_completed_corrections` returns `(corrections, status_flips)` (pure). The sync no longer hard-halts on this class; `assert_no_future_completed` remains only as a post-heal assertion. | UC25, UC27, ADR-039, ADR-069 | pytest evf.23, evf.44–evf.50 | Covered |
| FR-98 | Multi-slot event result URLs: `tbl_event` carries up to 5 nullable result-platform URL slots (`url_event`, `url_event_2..5`); admin enters them via the Event Edit form (slot #1 visible, slots #2–5 behind a disclosure with filled-count). On every save the 5 inputs are compacted (trim → drop empty → dedupe first-occurrence → pad NULL) so any non-null URL always lives in slot #1, preserving every existing `url_event`-as-primary code path (calendar 🔗 link, ⬇ Import button, ADR-029 auto-populate seed, ADR-028 EVF refresh write order). Tournament URL discovery iterates all non-null slots and merges per-(weapon,gender,category) results dedupe-first. `tbl_tournament.url_results` unchanged (drilldown leaf). Implemented as `fn_compact_urls(VARIADIC TEXT[]) RETURNS TEXT[]` shared by `fn_create_event` / `fn_update_event` / `fn_refresh_evf_event_urls`. | UC21, UC22(c), ADR-040 | 15.1–15.6, 9.44a–9.44f, pytest 3.16k–m, prom.8 | Covered |
| FR-99 | Server-side workflow dispatch: admin UI ⬇ buttons invoke a Supabase Edge Function (`dispatch-workflow`) that holds the GitHub PAT as a Supabase env secret and forwards `workflow_dispatch` calls. PAT never appears in HTML/JS bundles. Function uses an allowlist (`populate-urls.yml`, `scrape-tournament.yml`), verifies caller JWT (built-in Supabase auth), and returns sync `{ok, runs_url}`. Per-event inline status renders below the event-row in the admin accordion (pending → success-with-link → error). `github-pat` / `github-repo` HTML attributes removed from `<spws-ranklist>`. Telegram path (GAS server-side PAT) untouched. **N15 (2026-06-19):** new Telegram lifecycle command `ingest <prefix> <url> [cert\|prod]` dispatches `ingest-event.yml` via the same GAS→`workflow_dispatch` path as `promote`; the from-URL ingest sets `tbl_event.url_event` (admin), re-ingests (keep-rule + N14 `url_results`), and sends the staging `full.md`+`.diff.md` back to Telegram (reuses ADR-059 `send_staging_report`/`send_document`; `ingest_cli --send-telegram --md-target storage`). GAS snippet: [archive/legacy-2026-07/telegram-ingest-command-gas.md](archive/legacy-2026-07/telegram-ingest-command-gas.md). | UC22(c), UC27, ADR-041, ADR-025, ADR-059 | 9.45a–9.45f, N15.1–N15.4 | Covered |
| FR-100 | Season-init wizard with skeleton pre-allocation: admin creates a new season via a 3-step modal (Identity → Scoring → Confirm). On commit, `fn_create_season_with_skeletons` runs as one atomic transaction — INSERT season → overwrite default scoring config (carries `bool_show_evf_toggle`) → call `fn_init_season(p_id_season)` which produces one CREATED-status skeleton per recurring event kind (`^PPW\d+-` family, `^PEW\d+-` family, always-MPW, always-MSW, optional IMEW/DMEW per `enum_european_event_type`) plus 6 V2 child tournaments per skeleton. Cancel at any step = nothing persists (transaction rollback). Post-commit EDIT form shows skeletons as calendar-style boxes with "↶ Cofnij całość" link calling `fn_revert_season_init` (refuses if any skeleton has advanced past CREATED). EventManager exposes editable `txt_code` (cascades to children via `fn_update_event` v2) and `id_prior_event` picker. Supersedes carry-over admin runbook §3, §4, §6, §9. | UC22(a), ADR-044 | ph3.1–ph3.21 (pgTAP), ph3.23–ph3.37 (vitest wizard), ph3c.1–ph3c.12 (vitest EventManager) | Covered |
| FR-101 | Configurable carry-over engine selection per season (admin UI): `tbl_season.enum_carryover_engine` is exposed as a dropdown inside `ScoringConfigEditor` Section 4b "🔀 Silnik carry-over". Dropdown lists all `enum_event_carryover_engine` values dynamically (extensible — new engines auto-appear). Save flow patches `tbl_season` separately from `tbl_scoring_config` (instant flip, no migration). 🎯 Konfiguracja punktacji button rendered only on future + active seasons (past-complete seasons hide it; defense-in-depth via existing `readonly` prop). Greenfield seasons default to `EVENT_FK_MATCHING` after the Phase 3 column-DEFAULT flip; existing rows preserved. Supersedes carry-over admin runbook §5 ("flip via SQL"). | UC22(a), ADR-045, ADR-042 | ph3.22a/b/c (pgTAP), ph3.37a–g (vitest) | Covered |
| FR-102 | Unified Phase-5 ingestion pipeline: every XML/URL ingestion path routes through `run_pipeline` + DraftStore producing `tbl_*_draft` rows; direct writes to `tbl_result` from ingestion code are prohibited. Email-arrived XML and event-URL ingest produce identical artefacts (drafts + verdict `.md`) and require operator triage before commit. | ADR-050 (Phase 5.5 closeout), ADR-061 | 5.5 (md_writer), 5.7 (process_xml_file rewrite — pending) | Partial |
| FR-103 | Verdict `.md` persisted to Supabase Storage on CERT/PROD: every event ingestion produces `staging-reports/{event_code}/full.md` (replace-on-regen); EVF parity sweeps append `staging-reports/{event_code}/deltas/{ts}.md`. Bucket is private; service-role writes; authenticated reads. LOCAL fallback is filesystem `doc/staging/<event>.md` (storage disabled). | ADR-058 | 5.6 (storage_md), 5.12 (bucket RLS pgTAP) | Covered |
| FR-104 | Verdict `.md` delivered via Telegram document attachment: operator receives `.md` via Bot API `sendDocument` (multipart/form-data, MIME `text/markdown`) on first ingestion, post-triage regen, and EVF delta sweeps. Caption tags `kind` (full/delta) + contextual `extras` (tournament_count, pending_aliases, changes, reason) so operator can sort scrollback. Null-safe (skipped if no token/chat_id env vars on LOCAL). | ADR-059, ADR-061 | 5.9 (TelegramNotifier.send_document) | Covered |
| FR-105 | EVF parity sweep emits delta-only `.md` (only changed rows; never overwrites `full.md` from sweep); silent on no-drift. Per-event timestamped delta files at `staging-reports/{event_code}/deltas/{yyyyMMdd_HHmmss}.md` + Telegram document. `full.md` is owned by operator-driven actions only (first ingest, alias mutation regen). | ADR-060 | 5.8 (parity_delta renderer) | Partial (renderer covered; sweep wiring pending) |
| FR-106 | Alias triage UX: FencerAliasManager sorts unreviewed-aliases-first with amber visual highlight + "🔍 N" badge; auto-expand first unreviewed fencer on mount; "unreviewed only" filter checkbox. Modal-based "Create new fencer" replaces window.prompt chain (prepopulated with surname/firstname parsed + BY suggestion from V-cat × season-end-year). Cascade tournament list (id_tournaments[] + tournament_labels[] from extended RPCs) surfaced post-mutation in the status banner; cross-event ⚠ warning when prior signed-off events were touched. | ADR-050 (Phase 5.5), ADR-058+059 | 5.10 (vw_fencer_aliases ext), 5.11 (RPC tournament cascade), 5.1–5.4 (vitest — pending) | Partial |
| FR-107 | LOCAL operator workflow guarantee: `phase5_runner` / `phase5_report` default `--md-target=local` (filesystem write to `doc/staging/<event>.md`, no Storage upload, no Telegram); CERT/PROD CI workflows pass `--md-target=storage` explicitly. `dispatch-workflow` allowlist gains `phase5-event-runner.yml` + `regen-report.yml` for admin UI + GAS Telegram dispatch. | ADR-061 | 5.5 (md_writer target switch), L1–L6 LOCAL smoke | Covered |
| FR-108 | Telegram operator commands (extends ADR-025): `/regen <event_code>`, `/stage <event_code>`, `/parity <event_code>`, `/verdict <event_code>` — all dispatched from GAS `checkTelegramCommands` to GitHub workflows or Storage downloads. `/help` text rewritten to enumerate commands + describe auto-delivered documents (filename pattern, when fired). | ADR-061 | C16 (CERT smoke — manual) | Pending (GAS extension + smoke) |
| FR-109 | FTL event-schedule discovery skips brackets whose first significant token isn't in the SPWS-recognised vocabulary (Polish/English weapon, gender, V-cat, format markers); skipped entries surface in the per-event staging md with reason `"guest event (non-SPWS bracket name)"`. Closes the PPW4-2024-2025 AKADEMICKIE bug where a foreign competition shared the FTL schedule URL and produced duplicate `(id_fencer, id_tournament_draft)` pairs that blocked commit. Complementary to FR-? structural pool-round detection (ADR-057) — name whitelist at discovery, structural detection post-match. | ADR-062 | 5.21.1–5.21.4 (pytest `test_scrapers.py::TestFTLEventSchedule`) | Covered |
| FR-110 | FTL bracket-name parser tolerates Polish-grammar variants used by different SPWS organizers: `GENDER_MALE` accepts both genitive (`MĘŻCZYZN`, plain Z) and nominative (`MĘŻCZYŹNI`, with Ź) plural roots with optional plural suffix; `MIKST_PATTERN` recognises `GRUPY ZBIORCZE` as a pool-round indicator equivalent to MIKST/Mixed. A weapon-bearing bracket with **no gender keyword** (FT `Sexe="X"`, e.g. Polish `Szabla kat. 4` / `kat. 0`) defaults gender to `M` (ADR-34) instead of returning None, so the URL discovery path keeps the V0/V4 sabre brackets the XML path keeps — making URL ingest byte-equal to XML (2026-06-03). Closes the PPW5-2024-2025 silent-drop bug (14/26 brackets lost) and the PPW5-2025-2026 V0/V4 sabre drop. Regression-locked on the original genitive form. | ADR-063, ADR-34 | 5.22.1, 5.22.2, 5.22.2b (regression), 5.22.3, 3.15g2 (kat-no-gender → ADR-34 default) (pytest `test_scrapers.py::TestFTLEventSchedule`) | Covered |
| FR-111 | Min-participants threshold gates ingestion: tournaments with `n_competitors < threshold` are skipped at ingestion (no `tbl_tournament` row created). Threshold read per-season + per-type: PPW/MPW/PSW use `tbl_scoring_config.int_min_participants_ppw` (default 1); PEW/MEW/MSW use `int_min_participants_evf` (default 5). Strict less-than semantics: threshold=1 includes single-competitor walkover brackets; threshold=2 excludes them. Walkover semantics in FTL (`parse_ftl_json`, `parse_json`) and PPW1 XLSX parsers: when exactly one non-`excluded` entry has null/empty place, assign place=1 so the threshold can do its job. Staging summary lists every gated drop with name + weapon + gender + V-cat + n + reason + URL. Schema unchanged (columns existed since `20250301000001`). | UC22(f), ADR-066 | pytest 5.M3.1–5.M3.10 (`test_ftl_walkover.py`), 5.M4.1–5.M6.7 (`test_min_participants_helper.py`), 5.M7.1–5.M7.3 (`test_min_participants_summary.py`), 5.M9.1–5.M9.5 (`test_ppw1_xlsx_walkover.py`); pgTAP 5.M8.1–5.M8.4 (`42_min_participants_threshold.sql`) | Covered |
| FR-112 | **Rule-driven plugin ingestion architecture** (NEW design, [archive/legacy-2026-07/ingestion_pipeline_NEW_design.md](archive/legacy-2026-07/ingestion_pipeline_NEW_design.md)): three one-directional layers — RuleBook+RuleEngine resolve a named `Flow` into a DAG-validated `ExecutionPlan` *before* execution → generic Orchestrator runs it → Plugins (`IngestPlugin` contract reads/writes/effects + applies + run; kinds Source/Gate/Transform/Mutator/Reactor). Mutators emit signals; Reactors observe a signal → emit a Flow (no back-edges). Adding a scenario = adding a Rule, no orchestrator/plugin change. | ADR-073 (amends ADR-050) | N1.1–N1.18 (test_rule_engine.py, test_pipeline_core.py): planner flow→sequence, DAG-validation rejects mis-order, orchestrator skip/fault/abort/trace; N2.1 parity (new path == run_pipeline pctx); N7.1–N7.8 (test_commit_persist.py): Commit persists per V-cat bracket via fn_find_or_create_tournament + fn_ingest_tournament_results, per-V-cat own count (ADR-049 amend), method→legacy enum_match_status map, _skip_commit short-circuit, RECOMPUTE write-back deferred; live LOCAL end-to-end ingest writes tbl_result + scores; N8.1–N8.6 (test_ingest_cli_flow.py): ingest_cli `--flow ingest_domestic` parses via PARSERS + runs run_flow per file (no draft gate), per-file source_url, pool-only skip, unknown-event ValueError; live LOCAL parity (NEW run_flow vs OLD draft-commit) byte-matches scores on single-cat + combined pool | Covered |
| FR-113 | **`ResolveFencers` early auto-resolution, no human gate** (NEW design): merged Stage-0 ⊕ Stage-6 plugin owning *name → governed fencer*, runs first of the core; Phase A exact + BY reconcile (**promotion → band youngest edge, demotion → midpoint**, ADR-056 amend 2026-06-26; via shared `reconcile_fencer_birth_year`, not forked), Phase B fuzzy link-or-create by calibrated `AUTO_LINK_THRESHOLD`; asymmetric safety (create-over-uncertain-link). Name canonicalized (`canonicalize_scraped_name`) before the surname/first split so dirty FTL strings (`SAMECKA -NACZYŃSKA (1) Martyna`) don't spawn duplicates (ADR-065 amend). Replaces the ADR-050 review gate. | ADR-070 (amends ADR-050/056/065) | N3.1–N3.9 (test_resolve_fencers.py): exact-link/reconcile-BY/create/fuzzy-link/two-phase BY settling/calibration false-link bound/split-uses-governed-BY; promotion→youngest-edge + demotion→midpoint + cross-season stability (test_resolve_fencers + test_s0_reconcile_roster 10.4.1/10.4.2/10.4.2b); canonicalization + 5 dirty-variant AUTO_MATCH + over-merge regression (test_matcher.py) | Covered |
| FR-114 | **Master-data dedup sweep** (NEW design): `DEDUP_SWEEP` flow runs `ResolveFencers` whole-roster; `fn_merge_fencers` re-points results **and `tbl_match_candidate`** to the survivor (FK identity) + folds aliases; conflicting BYs reconciled (promotion → youngest edge, ADR-056 amend); merges emit `master_data.changed` → recompute. | ADR-071 | pgTAP 44.9–44.12 (re-points results + `tbl_match_candidate` + folds aliases + deletes dup; **44.12** = match_candidate FK re-point before dup delete, ADR-071 amend 2026-06-26); N5.5–N5.6 (DEDUP_SWEEP whole-roster merge) | Covered |
| FR-115 | **Self-healing recompute** (NEW design): a master-data change auto-triggers `RECOMPUTE_DOMESTIC` — re-derive V-cats + re-score the affected **event** from stored FK-linked results (no source, no re-match). Event-granular (relocation crosses brackets); column-aware CDC trigger + `tbl_recompute_queue` + debounced worker; convergent (Commit effects=live ⇒ no re-trigger). | ADR-072 | pgTAP 44.1–44.8 (column-aware trigger enqueues on BY/nationality, no-op on name; dedup coalesce; watermark); N4.1–N4.6 (recompute re-partitions by governed BY, boundary-crossing, twice==once); N5.1–N5.4 (debounce/claim/quiescence); **deterministic CLAIMED recovery** — `claim_recompute_batch` reclaims orphaned CLAIMED rows (PENDING+CLAIMED, single-writer; no stale-claim timeout, ADR-072 amend 2026-06-26) in `test_db_connector.py`; N9.1–N9.7 (test_recompute_persist.py): RECOMPUTE Commit write-back re-partitions by (weapon, gender, governed-V-cat), relocates across brackets, clears emptied brackets, skips BY-null rows; N5.7 (worker --drain CLI for the recompute-drain.yml scheduler); live LOCAL heal E2E (BY correction → CDC enqueue → debounced worker → fencer relocated V2→V3 + V2 re-scored on new field, no human action) | Covered |
| FR-116 | **No-hard-halt fault resolution** (NEW design): a domain problem is `ctx.fault(kind)`, never a halt; declarative `REMEDIATIONBOOK` applies an inline deterministic fix (drop_bracket / skip_bracket / accept_parsed / …) so the flow always reaches `Commit`; `Escalate` plugin fires Telegram only per policy (`ALWAYS`/`ON_LOSS`), post-commit, informational, never blocks. Reframes V0-international (exclude), below-min (drop), count-mismatch (flag) from halts to faults. | ADR-074 (reverses halt-by-exception in ADR-050/057/067; amends ADR-038/066/069) | N2.4–N2.9 (count-mismatch→accept+escalate, below-min→drop→commit, EVENT_NOT_RESOLVED→Abort, Escalate ALWAYS/ON_LOSS); N1.13/N1.14 (orchestrator fault-no-halt / Abort); N10.1–N10.7 (test_post_commit_chain.py): the PostCommit reactor chains POST_COMMIT after a committing flow, carrying event/faults/drops; a dropped below-min bracket auto-sends the ON_LOSS escalate Telegram, a clean ingest only the summary; converges (no back-edge, react=False on recurse) | Covered |
| FR-117 | **Source retention for dead-URL re-ingest** (NEW design): `ParseSource` persists `source_artifact_path`; an event whose live URL is dead can be re-ingested from retained bytes (`source=retained`). Distinct from recompute (which never touches source). | ADR-072 | N4.6 (ParseSource retains source_artifact_path for source=retained re-ingest) | Covered |
| FR-118 | **Staging report as a first-class plugin output** (NEW design): a fifth Context channel `report` (`ReportFragment(plugin, kind, section, payload)` + `ctx.add_report`/`BasePlugin.report`, bypasses write-discipline, every plugin kind emits) — every plugin serializes its own fragment as it runs, and one terminal **`StagingFormatter`** (Mutator/`docs`) shapes the merged fragments into `doc/staging/<EVENT>.<ts>.md` (from `STAGING_TEMPLATE`) + `.diff.md` (reuses `three_way_diff`). Event-scoped POST_COMMIT tail (CLI `_fire_staging_report` seeds `_bracket_reports`; per-bracket reactor fire SKIPs it). Timestamped filenames compare reruns. Informational/post-commit — restores the review artifact ADR-074 removed, no drafts. | ADR-075 (restores ADR-050 artifact under ADR-074; reuses ADR-058/061) | N11.1–N11.7 (test_staging_report.py): report channel + add_report tagging/write-discipline-bypass, BasePlugin.report name+kind, every plugin emits its fragment in a real flow, StagingFormatter applies-at-event-scope, .md template order + schedule skips, .diff.md via three_way_diff, CLI fires one event-scoped POST_COMMIT + timestamped stems; **N12.1–N12.7 (richness parity, 2026-06-17): DetectPoolRound name/reason/url + ResolveFencers alias_writebacks + per-created near-miss; rich automation-report renderers reusing `_fetch_event_meta`/`_live_tournament_rows`/`_check_pool_round_count`/`_classify_alias_pair` (full event header, automation-gaps centerpiece, created+near-miss, BY-reconcile cols, matching paths/verdicts/BY-quality/NULL+estimated tables w/ bracket+place, deduped committed table, present-but-omitted brackets, parse-status +reasons, no sign-off)** | Covered |
| FR-119 | **Overlapping-FTL-listing dedup (overlap-clobber fix)** (NEW design): several rounds resolving to the same `event·weapon·gender·age_category` overwrote each other (delete-reimport). **Rule 1** drops pools-only rounds (no DE/Tableau) on the from-URL path + fixes the bogus bundled skip reasons; **Rule 2 keep-rule** (`_resolve_sources`) keeps one source per category (dedicated single > BRACKET; else smaller BRACKET), `Commit` honours a per-listing `commit_cats` allow-set (no double-write); set-aside duplicates flagged (never stalled/merged). Discovered rounds + status persist on `tbl_event.json_ingest_sources` (display-only, never scored) + admin `json_source_overrides` (`fn_set_event_source_override`, on `vw_calendar`); the event accordion shows committed/dropped/skipped rows + skip/process toggle + **Re-ingest** button (`ingest-event.yml`). | ADR-076 (extends ADR-067; ADR-014/022/025/006/041; surfaces via ADR-075) | N13.1 (`_ftl_has_direct_elimination` + accurate skip reasons), N13.2 (`commit_cats` filter), N13.3 (`_resolve_sources` keep-rule + Rule 2b + override), N13.4 pgTAP 45.1–45.6 (`tbl_event` JSONB + RPC + `vw_calendar`), N13.5 (staging duplicate flags + per-source split), N13.6 vitest (accordion source rows + toggle); dry-run validated PPW3/PPW4/PPW5 | Covered |
| FR-120 | **`tbl_registration`** (ephemeral, public self-registration record): `id_event`, nullable `id_fencer` (matched fencer, NULL until/unless resolved), declared `txt_surname`/`txt_first_name`/`enum_gender`/`int_birth_year`, `arr_weapons`, computed `txt_ftl_name` (canonical name + `(N)` marker), `ts_consent`/`txt_consent_version`, salted `txt_email_hash` (abuse log only, never the raw email), `ts_created`. **No payment-status column** — this system displays bank-transfer info but does not track whether it's paid; that's checked in person at the venue (corrected 2026-07-04). `UNIQUE(id_event, id_fencer)` enables anonymous-path upsert (NULLs distinct, so unmatched registrations never collide). | ADR-079 §5 | 49.1–49.6, 49.20–49.21 (pgTAP: table + columns + no-payment-column regression guard + upsert-not-duplicate + NULLs never collide) | Covered |
| FR-121 | **`tbl_event` registration/delivery columns**: `url_entry_list`, `txt_organizer_email`, `ts_ftl_sent`, `num_entry_fee_2w`, `num_entry_fee_3w`, `bool_use_spws_registration` (`url_registration`/`dt_registration_deadline` already existed, ADR-030). | ADR-080 §5, ADR-030 | 49.7–49.12 (pgTAP: columns present) | Covered |
| FR-122 | **RLS — no direct public writes to `tbl_registration`**: no anon INSERT/UPDATE/DELETE row policy exists (implicit deny); the only public write path is a `SECURITY DEFINER` RPC (`fn_create_registration`) which is the sole `anon`-executable entry point. **Consent write path (Phase 2 P2.0, added 2026-07-05):** `fn_create_registration` gains `p_consent_version TEXT DEFAULT NULL` — stamps `ts_consent = now()` + `txt_consent_version` when given (the frontend passes it at RODO-accept, D5); NULL leaves both unchanged (legacy/no-consent callers unaffected). **Registration-window guard (D10):** the same RPC `RAISE`s once `COALESCE(dt_registration_deadline, dt_start)` is in the past, so a stale/expired shared link can no longer write a registration; NULL-date events (open, incl. existing pgTAP fixtures) stay unaffected. | ADR-079 §1 (read-only invariant), ADR-079 §5, ADR-078 §7 (consent versioned+timestamped) | 49.13–49.14 (pgTAP: direct anon write denied, RPC succeeds); **49.23–49.24** (consent stamped when given; past-deadline write rejected) | Covered |
| FR-123 | **Public entry-list view** (`vw_registration_entry_list`): surname/first name/gender/weapons/**age category** per registration, **every declared registration, no payment gate** (corrected 2026-07-04 — this system does not track payment completion; the organizer verifies it in person at the venue) — explicitly **excludes birth year and club** (GDPR minimisation, matches the public roster mockup). **Category column/filter added 2026-07-05** — `enum_age_category` (via `fn_age_category`, joining `tbl_event`/`tbl_season` for the season end year) now exposed on the view and rendered as a badge + filter in `EntryList.svelte` (matching `FilterBar.svelte`'s V0 (30+)…V4 (70+) option convention), closing a gap where the view/component previously omitted it despite the mockup showing it. **Mobile layout fix (same day, user-reported on-device regression):** the ≤480px row layout previously collapsed every `<td>` to its own block (5 stacked lines per fencer); `EntryList.svelte` now renders one non-wrapping flex row per fencer (name left, ellipsis-truncated; category+weapon badges packed right). Dropped the redundant `Lp` (row number) and `Gender` display columns (gender stays filterable, just not shown as a column) — surname/first name make gender/Lp redundant. The three filter dropdowns (weapon/category/gender) now sit in a 3-column grid, each under its own `<label>` (`Broń`/`Kategoria`/`Płeć`) with a `--` placeholder option, replacing the old folded-in "Weapon: all" style option text; select font bumped to 16px (legible + avoids iOS auto-zoom-on-focus). View column list and `enum_gender` filter semantics unchanged. Readable by `anon` (view owner bypasses the base table's restrictive RLS, the same pattern used by every other `vw_*` in this codebase). | ADR-079 §5, ADR-078 (minimisation), spec §5.2 invariants | 49.15–49.17, 49.17b, 49.25–49.27 (pgTAP: view excludes BY/club, anon SELECT succeeds, unpaid/PENDING registration still appears, category column type + computed value + BY-still-excluded regression guard); vitest `EntryList.test.ts` 12 total (8→11, +3 category badge/filter/options 2026-07-05 AM; 11→12, +1 filter-label/placeholder convention 2026-07-05 PM mobile fix) | Covered |
| FR-124 | **Exact-tuple match RPC** (`fn_match_registration_fencer`): read-only, case/space-normalised exact `(surname, first_name, birth_year)` lookup against `tbl_fencer` — the **complete** form-side identity router. Exact triple → ADR-079 Path A (skip-email fast path); **any** miss (unknown, near-miss typo, right-name/wrong-BY) → the email-verify path. The form does **no** fuzzy matching: ADR-079's Paths B/C/D are an **ingestion-time** reconciliation model realised by the **existing** Python `find_best_match` (never invoked synchronously from the browser), so the "invocation gap" closes by construction — no Python-from-frontend bridge is needed (ADR-079 §2 implementation note, resolved 2026-07-05; supersedes the earlier "OPEN Phase 2 design question"). | ADR-079 §2 (Model B paths + implementation note) | 49.18–49.19, 49.22 (pgTAP: exact match found/not found; strong-name near-miss → NULL → email path) | Covered (form-side exact routing complete; B/C/D reconciled at ingestion) |
| FR-125 | **Canonical seed-name formatting**: `to_canonical_name` (surname UPPERCASE, given name Title case, fixes legacy all-caps given names) + `format_prenom_with_marker` (appends the `(N)` age-category digit). | ADR-080 §1 | `python/tests/test_ftl_seed_export.py` — 7 tests (basic/hyphenated/legacy-caps/whitespace/marker) | Covered |
| FR-126 | **Mix-all pool interleave** (`interleave_mixall`): round-robin "snake by rank" across the 10 domestic sub-rankings in fixed order `FV0,FV1,FV2,FV3,FV4,MV0,MV1,MV2,MV3,MV4`, empty sub-rankings skipped. | ADR-080 §2 | `test_ftl_seed_export.py` — 3 tests (worked-example order incl. skip-empty, empty input, all-empty) | Covered |
| FR-127 | **Predicted combined DE brackets** (`predict_combined_brackets`): per weapon×gender (never merged across gender), order V0→V4, skip empties, accumulate left-to-right, close a bracket at running count ≥ T=4, fold a trailing sub-T bracket into the previous one (or make it the sole bracket if none precede). | ADR-080 §3 | `test_ftl_seed_export.py` — 5 tests (exact-close, skip-empty adjacency, trailing-fold, whole-under-T degenerate case, all-empty) | Covered |
| FR-128 | **Seed file naming** (`seed_filename`, `combined_bracket_scope`): `<season>_<eventcode>_<weapon>_<scope>.xml`, `<scope>` = `mixall` or `<G>-<cat>` (single or compact-joined combined, e.g. `M-V0V1`). | ADR-080 §4 | `test_ftl_seed_export.py` — 5 tests (mixall, single DE, combined DE, scope builder ×2) | Covered |
| FR-129 | **FIE XML generation** (`build_fie_xml`): `<BaseCompetitionIndividuelle>` root (`Championnat=SPWS`, `Federation=POL`, `Date=""`) + `<Tireur>` children — omits `DateNaissance` and `Lateralite` entirely, `Club=""`/`Licence=""` (not collected), canonical `Nom`/`Prenom` with `(N)` marker preserved, tireur order preserved. | ADR-080 §1 | `test_ftl_seed_export.py` — 4 tests (omitted fields, root attrs, tireur attrs, multi-tireur order) | Covered |
| FR-130 | **Public registration UI (Phase 2)**: standalone shareable page `frontend/register.html` (own URL, zero dependency on calendar/main app) renders `<spws-registration event="…">` (identity → exact-match routing → RODO consent gate → payment, click-to-copy) or `<spws-entry-list event="…">` (`?view=list`) — custom elements built by `vite.config.ce.ts`. **Expiry gating (D10, client-side mirror of the FR-122 server guard):** `today ≤ COALESCE(dt_registration_deadline, dt_start)` → form open; past cutoff but `≤ dt_end` → "Zapisy zamknięte" (list still viewable); past `dt_end` → whole page expired, no form/list. `bool_use_spws_registration=FALSE` → external `url_registration` link only (self-enforced, so a shared link is always safe). Mobile-first (D8): single-column reflow ≤480px, no horizontal scroll, ≥36px touch targets. **RODO checkbox mobile fix + invalid-state cue (same day, user-reported on-device regression):** the global `input, select { width: 100% }` rule was stretching the RODO `<input type="checkbox">` to fill its flex row, pushing the consent label off-screen on narrow viewports — `.reg-rodo-checkbox` now overrides to a fixed custom-styled 20×20px box (`appearance: none`, own checked/unchecked paint) so D8's "no horizontal scroll" holds on the consent step too. The accept button no longer hard-`disabled`s on an unticked box (which silently swallowed the click with no feedback); clicking without consent now sets `consentInvalid` — the checkbox fills light red (`rgba(255,92,92,.45)` background + red border) until the box is ticked, then clears automatically. **Calendar integration (P2.8, amended ADR-079 §7 2026-07-05):** admin `EventManager.svelte` "Rejestracja SPWS" section (toggle + 2/3-weapon fee tiers) writes via extended `fn_update_event`, which now also auto-derives and persists absolute `url_registration`/`url_entry_list` (`fn_update_event p_url_entry_list`, migration `20260705000004`) so a cross-origin embed is self-sufficient (no `registration-base` attribute — that plumbing was removed). `CalendarView.svelte` renders both links from the stored columns and opens them in an in-app `RegistrationModal.svelte` overlay (DrilldownModal-style; `RegistrationForm`/`EntryList` gained `onclose`/`onviewlist` props) instead of navigating away; `href` is kept for copy-link/open-in-new-tab. `bool_use_spws_registration=FALSE` still keeps the existing external `url_registration` link unchanged, plain navigation (add-only, ADR-080 §5 UI constraint). | ADR-079 §6 (UI), §7 (URL auto-fill + modal, 2026-07-05 amend), ADR-080 §5 (add-only constraint), ADR-078 (RODO notice/consent) | vitest: `RegistrationForm.test.ts` (18: +2 accept-without-consent flags invalid / ticking clears invalid + writes, 2026-07-05 PM mobile fix), `EntryList.test.ts` (12, see FR-123), `birthYearEstimate.test.ts` +8 (`birthYearToVcat`), `api.registration.test.ts` +2 (`fetchEventForRegistration`), `EventManager.test.ts` +3, `CalendarView.test.ts` +2 (+2 ADR-079§7: stored-URL links, modal open/close); `RegistrationModal.test.ts` (5, new); pgTAP 8.23 (`fn_update_event` registration fields) + 8.24 (`p_url_entry_list`); LOCAL E2E (exact-match fencer → RODO accept → `tbl_registration` row with `ts_consent`/`txt_consent_version` → appears in entry list) | Covered |
| FR-131 | **Organizer FTL seed delivery:** authenticated admin UI and allowlisted Telegram command dispatch `ftl-seed.yml` for an explicit CERT/PROD event; Python generates a fresh, non-empty mix-all ZIP in memory, sends it as the SPWS Gmail over verified `SMTP_SSL`/465, and stamps `ts_ftl_sent` only after SMTP acceptance through service-role-only `fn_mark_ftl_sent`. Manual dispatch is an explicit re-send. The daily PROD deadline sweep is at-least-once, selects only unstamped live events after cutoff, skips empty bundles, serializes with `prod-write`, and remains disabled until `ENABLE_FTL_DEADLINE_SEND=true` after the CERT pilot. | ADR-080 §5, spec §5.2 | `FTLDEL-PY-01`–`FTLDEL-PY-07` (`test_ftl_feed_seed_send.py`, exporter/orchestration compatibility); `FTLDEL-DB-01`–`FTLDEL-DB-03` (pgTAP 49.28–49.33, 8.25); `FTLDEL-UI-01`–`FTLDEL-UI-02` (`EventManager.test.ts`); `FTLDEL-OPS-01` (`test_ftl_seed_workflow_contract.py`) | Implemented; CERT pilot pending |
| FR-132 | **Workflow dispatch endpoint authenticates its caller and requires MFA.** `dispatch-workflow` rejects any request whose bearer token is absent, undecodable, or carries a `role` claim other than `authenticated` — closing the finding that `verify_jwt = true` was satisfied by the **public anon key** (it ships in the frontend bundle), so any visitor could dispatch `promote-season.yml` or `ftl-seed.yml` against PROD. The role check precedes token resolution so the hostile case costs no network round-trip; `aal2` is checked last so a genuine admin who has not completed TOTP receives `403 mfa_required`, distinct from `401`. Decision logic is isolated in `authorize.ts` (no Deno/network dependency) so it is unit-testable rather than only reachable through a deployed function. | ADR-083 §5, ADR-041, ADR-016 | vitest FR-132.1–FR-132.9 (`dispatchWorkflowAuth.test.ts`) | Covered |
| FR-133 | **Public-schema objects grant no access to `anon` by default; the anon surface is an explicit allowlist.** `ALTER DEFAULT PRIVILEGES FOR ROLE postgres` no longer grants `anon`/`authenticated` on new TABLES, FUNCTIONS or SEQUENCES — the generator that silently re-opened the March 2026 REVOKE (`20260327000001`) once per new object. Existing exposure closed in the same migration: RLS + full revoke on the six pipeline/staging tables, `anon` write privileges removed from all 16 tables, `vw_fencer_aliases` (owner-rights, 365 identities) revoked from `anon`, and EXECUTE revoked from `anon, PUBLIC` on 13 write-capable RPCs incl. `fn_merge_fencers` and `fn_refresh_active_season`. The 19 surviving anon-callable functions are pinned as a set **equality** — a deny-list would have caught none of the findings. `fn_create_registration` / `fn_match_registration_fencer` are deliberately retained (ADR-079/FR-122 public self-registration). | ADR-083 §1–§4, ADR-016, ADR-078 | pgTAP 52.1–52.12 (`52_security_posture.sql`); `scripts/check-security-posture.sh` | Covered |

> **Note:** FR-69 was retired before assignment; gap is intentional. FR-102–FR-108 added Phase 5.5 (2026-05-03). FR-109 added Phase 5.5 follow-up (2026-05-03, ADR-062). FR-110 added same day (2026-05-03, ADR-063). FR-111 added 2026-05-10 (ADR-066). **FR-112–FR-117 added 2026-06-14 (NEW pipeline design, ADR-070–074); implemented + tested 2026-06-15 (build M1–M5) — now Covered.** **FR-118 added 2026-06-16 (staging report, ADR-075); implemented + tested same day — Covered.** **FR-119 added 2026-06-18 (overlap-clobber dedup, ADR-076); implemented + tested same day — Covered.** **FR-120–FR-124 added 2026-07-04 (event registration Phase 1 — DB schema, ADR-079/080); implemented + tested same day — Covered. FR-124 invocation-gap resolved 2026-07-05: the form-side matcher is exact-only (Path A vs email-verify); fuzzy B/C/D reconciliation stays at ingestion (existing Python `find_best_match`), so no Python-from-frontend bridge is needed — pgTAP 49.22 pins the contract (ADR-079 §2 implementation note).** **FR-125–FR-129 added 2026-07-04 (event registration Phase 3 — FTL seed export pure logic, ADR-080); implemented + tested same day — Covered. DB-querying orchestration layer built 2026-07-05 (`python/pipeline/ftl_seed_export.py` pure functions `registration_subranking_key`/`assemble_mixall_subrankings`/`mixall_tireurs`/`build_event_mixall_files`/`bundle_seed_zip` + thin Supabase glue `python/pipeline/ftl_seed_export_db.py::FtlSeedExporter`): POPULATION = every declared `tbl_registration` row (no payment gate), ORDERING from `fn_ranking_ppw` (ranked-then-unranked-by-`ts_created`), `(N)` from the DECLARED BY — `python/tests/test_ftl_seed_orchestration.py`, 15 tests, pytest 1003→1018. Mix-all seed files match the validated reference `doc/external_files/FTL_SRC/SPWS_ppw_epee_mixall_noBY.xml`. Per-bracket DE seed files deferred (no validated DE reference; `predict_combined_brackets`/`combined_bracket_scope` ready for when there is one).** **FR-130 added 2026-07-05 (event registration Phase 2 — public registration UI + calendar integration, ADR-079/080); implemented + tested same day — Covered. FR-131 added 2026-07-13 (organizer seed delivery, ADR-080 §5); implementation complete with CERT pilot and PROD cron enablement pending.** Total active FRs = 130 assigned; 129 covered + 1 implemented/pilot-pending.

## Non-Functional Requirements

| ID | Requirement | Source | Tests | Status |
|----|-------------|--------|-------|--------|
| NFR-01 | API response < 500 ms; scoring engine < 2 s per tournament | §10 | — | Not tested |
| NFR-02 | 99.9% availability via Supabase managed hosting | §10 | — | Infrastructure (not testable) |
| NFR-03 | Storage < 100 MB for 5 seasons | §10 | — | Not tested |
| NFR-04 | 50 concurrent users | §10 | — | Not tested |
| NFR-05 | RLS: anon read-only on public tables | §9.2.1 | 1.10a–b, 1.25 | Covered |
| NFR-06 | RLS: authenticated full CRUD | §9.2.1 | 1.11 | Covered |
| NFR-07 | RLS: audit log not publicly readable | §9.2.1 | 1.26 | Covered |
| NFR-08 | Browser compatibility (Chrome, Firefox, Safari, Edge) | §10 | — | Not tested |
| NFR-09 | Mobile responsive ≥ 375 px | §10, ADR-032 | C.1–C.14 | Covered (drilldown cards) |
| NFR-10 | Pipeline observability (structured logs + Telegram) | §10 | 3.6 (Telegram only) | Partial |
| NFR-11 | Database migration strategy (numbered SQL files) | §9.9 | 1.1a–1.2i (schema verification) | Covered |
| NFR-12 | Data integrity (Supabase daily backups) | §10 | — | Infrastructure (not testable) |
| NFR-13 | Shadow DOM isolation (host CSS does not leak) | §5, §6 | 8.55–8.61 | Covered (M8) |
| NFR-14 | Security posture machine-verified against the **live** environment on every release, blocking the release on regression and alerting via Telegram | ADR-083 §6 | pgTAP 52.1–52.12; `scripts/check-security-posture.sh` run in `release.yml` deploy-cert/deploy-prod | Covered |

## Coverage Summary

| Status | Count | FRs |
|--------|-------|-----|
| Covered | 99 | FR-01–FR-52, FR-55–FR-58, FR-59–FR-68, FR-70–FR-86, FR-88–FR-101, FR-103, FR-104, FR-107, FR-109, FR-110 |
| Partial | 5 | FR-53, FR-54, FR-102, FR-105, FR-106 |
| Pending | 1 | FR-108 (GAS extension + CERT smoke) |
| Superseded | 1 | FR-87 (by FR-88) |
| Not tested (NFR) | 4 | NFR-01, NFR-03, NFR-04, NFR-08 |
| Infrastructure (NFR) | 2 | NFR-02, NFR-12 |
| Covered (NFR) | 7 | NFR-05, NFR-06, NFR-07, NFR-09, NFR-10 (partial), NFR-11, NFR-13 |

## Rebuild Infrastructure (Phase 2b — ADR-050 / ADR-055)

The rebuild introduces infrastructure tests that don't map to user-facing FRs. They are catalogued here for traceability against the architecture decisions that introduced them. For the rebuild's per-sub-phase status, see [doc/archive/legacy-2026-07/development_history.md](archive/legacy-2026-07/development_history.md) §Phase 2b.

| ADR | Scope | Tests | Status |
|-----|-------|-------|--------|
| [ADR-050](adr/050-unified-ingestion-pipeline.md) | Unified Ingestion Pipeline — IR contract + 8 parsers + parser registry (Phase 1); draft tables + dry-run loop + commit/discard RPCs (Phase 2); pipeline body S1-S7 + override system + alias writeback + 3-way diff + interactive review CLI (Phase 3) | **Phase 1**: `python/tests/test_ir.py::ir.1`–`ir.7`; `python/tests/test_ir_contracts.py` (41 parser-contract assertions across 9 test classes). **Phase 2**: `supabase/tests/27_draft_tables.sql::27.1`–`27.26` (30 pgTAP); `python/tests/test_draft_store.py::P2.D1`–`P2.D9` (9); `python/tests/test_draft_diff.py::P2.M1`–`P2.M5` (5); `python/tests/test_ingest_cli.py::P2.C1`–`P2.C6` (6). **Phase 3**: `supabase/tests/28_alias_writeback.sql::28.1`–`28.8` (8 pgTAP — fn_update_fencer_aliases NULL init, dedup, trim, empty-reject); `python/tests/test_pipeline_types.py::P3.T1`–`P3.T8` (8 — HaltError + Overrides + PipelineContext shape); `python/tests/test_overrides.py::P3.OV1`–`P3.OV15` (15 — 5-surface YAML parser); `python/tests/test_pipeline_stages.py::P3.RP.x` + `P3.S1.x`–`P3.S7.x` (29 — dispatcher + per-stage isolation tests for S1-S7); `python/tests/test_three_way_diff.py::P3.D1`–`P3.D12` (12 — 4-bucket classifier + histogram + markdown); `python/tests/test_review_cli.py::P3.RC1`–`P3.RC12` (15 incl. 3 EVF-API-path tests — interactive review lifecycle); `python/tests/test_pipeline_integration.py::P3.INT.1`–`P3.INT.4` (4 — end-to-end against live LOCAL Supabase, skip-on-unreachable) | Covered (Phase 1 2026-05-01; Phase 2 + Phase 3 2026-05-02) |
| [ADR-055](adr/055-ingest-traceability.md) | Per-parser provenance stamps on `tbl_event` + `tbl_tournament`; cap-6 history tables; 8 design decisions D1–D8 | `supabase/tests/26_ingest_traceability.sql::26.1`–`26.23` (enum + stamp columns + history tables + FK CASCADE + UNIQUE + cap-of-6 trigger + per-parent isolation); also exercised by Phase 2's `27.18` (commit appends history with run_id) | Covered (Phase 1, 2026-05-01) |
| ADR-054 (planned) | Carry-over FK + 366-day cap | TBD — Phase 7 | Pending |

ADR-051 (reserved-but-empty), ADR-052, and ADR-053 are catalogued in the spec's Appendix C — Architecture Decision Registry, not duplicated here.

The cross-language enum-sync invariant (Python `SourceKind` ↔ Postgres `enum_parser_kind`) is enforced at runtime by `test_ir.py::test_source_kind_matches_postgres_enum` — drift detector for any future source addition.

## Cross-references

- ADR registry: Project Specification Appendix C — Architecture Decisions
- Test baseline (counts per suite): Project Specification Appendix D
- Use cases: Project Specification §3
- Functional scope: Project Specification §6 (Implementation Phasing) → externalized to [doc/archive/legacy-2026-07/development_history.md](archive/legacy-2026-07/development_history.md)
