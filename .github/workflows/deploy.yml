name: Deploy CERT to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Inject CERT Supabase credentials
        working-directory: frontend
        run: |
          sed -i 's|supabase-url="[^"]*"|supabase-url="${{ secrets.SUPABASE_URL }}"|' index.html
          sed -i 's|supabase-key="[^"]*"|supabase-key="${{ secrets.SUPABASE_ANON_KEY }}"|' index.html

      - name: Build with relative base path
        working-directory: frontend
        run: npx vite build --base ./

      - name: Verify no secrets or localhost in build
        run: |
          ! grep -r 'service_role' frontend/dist/
          ! grep -r '127.0.0.1' frontend/dist/
          ! grep -r 'supabase-demo' frontend/dist/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
