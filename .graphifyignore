doc/archive/

# Generated documentation HTML is a build artifact, not source. The Markdown
# sources ARE the graph corpus; their rendered .html twins (produced by
# scripts/render_adrs.py and scripts/render_docs.py) duplicate that content and
# must not be re-extracted into the knowledge graph. See ADR-082.
# NOTE: these files are still committed to git (the ADR-082 source→artifact
# contract) — this only keeps them out of graphify, never out of version control.
# Only these two top-level HTML files are generated from retained Markdown.
# Do not use `doc/*.html`: Graphify's matcher treats `*` as recursive and would
# incorrectly exclude authored pages under doc/handbook/, governance/, etc.
doc/Project Specification. SPWS Automated Ranklist System.html
doc/requirements-traceability-matrix.html
doc/adr/*.html

# Generated CERT→PROD reconcile run logs — one pair per day, pure churn. They
# are operational records, not source, and add ~22 low-value doc nodes that the
# semantic extractor would otherwise spend LLM budget on. (doc/archive/ above
# already excludes superseded prose; this covers the live staging logs.)
doc/staging/

# MŚW Tbilisi campaign correspondence. This one file links named individuals to
# private campaign replies, and the campaign promised those contacts would be
# deleted once entries close (privacy variant A). It is deliberately NOT tracked
# in git; keep it out of the local knowledge graph too, so a grep of graph.json
# can never surface it. The other MŚW/XLS plans carry no personal data and may
# be indexed normally.
doc/plans/msw-tbilisi-handover-2026-07-19-evening.html
